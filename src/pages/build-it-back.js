import React, { Component } from "react";
import Layout from "../components/layout";
import PageTitle from "../components/pageTitle";

export class build_it_back extends Component {
  render() {
    return (
      <Layout>
        <PageTitle title="Build-It-Back" />
      </Layout>
    );
  }
}

export default build_it_back;
