import React, { Component } from "react";
import Layout from "../components/layout";
import PageTitle from "../components/pageTitle";

export class pipe_organ extends Component {
  render() {
    return (
      <Layout>
        <PageTitle title="Pipe Organ Fund" />
      </Layout>
    );
  }
}

export default pipe_organ;
